{% extends "layouts/admin.twig" %}
{% block title %}Machines{% endblock %}
{% block page_title %}Machines{% endblock %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <div class="btn-group" role="group">
        <a href="?view=list{{ filters.status ? '&status=' ~ filters.status }}{{ filters.customer_id ? '&customer_id=' ~ filters.customer_id }}{{ filters.machine_type_id ? '&machine_type_id=' ~ filters.machine_type_id }}{{ filters.search ? '&search=' ~ filters.search }}" class="btn btn-outline-secondary {{ view != 'grid' ? 'active' }}" title="List View">
            <i class="bi bi-list"></i>
        </a>
        <a href="?view=grid{{ filters.status ? '&status=' ~ filters.status }}{{ filters.customer_id ? '&customer_id=' ~ filters.customer_id }}{{ filters.machine_type_id ? '&machine_type_id=' ~ filters.machine_type_id }}{{ filters.search ? '&search=' ~ filters.search }}" class="btn btn-outline-secondary {{ view == 'grid' ? 'active' }}" title="Grid View">
            <i class="bi bi-grid"></i>
        </a>
    </div>
    <a href="/machines/create" class="btn btn-primary">
        <i class="bi bi-plus-lg me-1"></i> Add Machine
    </a>
</div>

<!-- Filters -->
<div class="card border-0 shadow-sm mb-4">
    <div class="card-body">
        <form method="GET" action="/machines" class="row g-3 align-items-end">
            <div class="col-md-2">
                <label class="form-label">Status</label>
                <select name="status" class="form-select">
                    <option value="">All Statuses</option>
                    <option value="active" {{ filters.status == 'active' ? 'selected' }}>Active</option>
                    <option value="maintenance" {{ filters.status == 'maintenance' ? 'selected' }}>Maintenance</option>
                    <option value="inactive" {{ filters.status == 'inactive' ? 'selected' }}>Inactive</option>
                    <option value="in_storage" {{ filters.status == 'in_storage' ? 'selected' }}>In Storage</option>
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label">Customer</label>
                <select name="customer_id" class="form-select">
                    <option value="">All Customers</option>
                    {% for customer in customers %}
                        <option value="{{ customer.id }}" {{ filters.customer_id == customer.id ? 'selected' }}>{{ customer.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label">Machine Type</label>
                <select name="machine_type_id" class="form-select">
                    <option value="">All Types</option>
                    {% for type in machine_types %}
                        <option value="{{ type.id }}" {{ filters.machine_type_id == type.id ? 'selected' }}>{{ type.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label">Search</label>
                <input type="text" name="search" class="form-control" placeholder="Search machines..." value="{{ filters.search }}">
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-outline-primary w-100">
                    <i class="bi bi-funnel me-1"></i> Filter
                </button>
            </div>
        </form>
    </div>
</div>

{% if view == 'grid' %}
<!-- Grid View -->
<div class="row g-3">
    {% for machine in machines %}
    <div class="col-sm-6 col-lg-4 col-xl-3">
        <div class="card border-0 shadow-sm h-100">
            {% if machine.photo_url %}
                <img src="{{ machine.photo_url }}" class="card-img-top" alt="{{ machine.name }}" style="height: 180px; object-fit: cover;">
            {% else %}
                <div class="card-img-top bg-light d-flex align-items-center justify-content-center text-muted" style="height: 180px;">
                    <i class="bi bi-joystick" style="font-size: 3rem;"></i>
                </div>
            {% endif %}
            <div class="card-body">
                <h6 class="card-title mb-1">
                    <a href="/machines/{{ machine.id }}" class="text-decoration-none">{{ machine.name }}</a>
                </h6>
                <small class="text-muted">{{ machine.machine_code }}</small>
                <div class="mt-2">
                    {% if machine.status == 'active' %}
                        <span class="badge bg-success">Active</span>
                    {% elseif machine.status == 'maintenance' %}
                        <span class="badge bg-warning text-dark">Maintenance</span>
                    {% elseif machine.status == 'inactive' %}
                        <span class="badge bg-secondary">Inactive</span>
                    {% elseif machine.status == 'in_storage' %}
                        <span class="badge bg-info">In Storage</span>
                    {% endif %}
                    {% if machine.type_name %}
                        <span class="badge bg-light text-dark">{{ machine.type_name }}</span>
                    {% endif %}
                </div>
                <div class="mt-2 small text-muted">
                    {% if machine.customer_id %}
                        <i class="bi bi-building me-1"></i><a href="/customers/{{ machine.customer_id }}">{{ machine.customer_name }}</a>
                    {% else %}
                        <i class="bi bi-building me-1"></i>Unassigned
                    {% endif %}
                </div>
                {% if machine.location_details %}
                    <div class="small text-muted"><i class="bi bi-geo-alt me-1"></i>{{ machine.location_details }}</div>
                {% endif %}
            </div>
            <div class="card-footer bg-transparent border-top-0 text-end">
                <a href="/machines/{{ machine.id }}" class="btn btn-sm btn-outline-primary" title="View"><i class="bi bi-eye"></i></a>
                <a href="/machines/{{ machine.id }}/edit" class="btn btn-sm btn-outline-secondary" title="Edit"><i class="bi bi-pencil"></i></a>
            </div>
        </div>
    </div>
    {% else %}
    <div class="col-12">
        <div class="text-center text-muted py-4">No machines found.</div>
    </div>
    {% endfor %}
</div>
{% else %}
<!-- List View -->
<div class="card border-0 shadow-sm">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Machine Code</th>
                        <th>Name</th>
                        <th>Customer</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Location</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for machine in machines %}
                    <tr>
                        <td><strong>{{ machine.machine_code }}</strong></td>
                        <td>{{ machine.name }}</td>
                        <td>
                            {% if machine.customer_id %}
                                <a href="/customers/{{ machine.customer_id }}">{{ machine.customer_name }}</a>
                            {% else %}
                                <span class="text-muted">Unassigned</span>
                            {% endif %}
                        </td>
                        <td>{{ machine.type_name|default('--') }}</td>
                        <td>
                            {% if machine.status == 'active' %}
                                <span class="badge bg-success">Active</span>
                            {% elseif machine.status == 'maintenance' %}
                                <span class="badge bg-warning text-dark">Maintenance</span>
                            {% elseif machine.status == 'inactive' %}
                                <span class="badge bg-secondary">Inactive</span>
                            {% elseif machine.status == 'in_storage' %}
                                <span class="badge bg-info">In Storage</span>
                            {% endif %}
                        </td>
                        <td>{{ machine.location_details|default('--') }}</td>
                        <td class="text-end">
                            <a href="/machines/{{ machine.id }}" class="btn btn-sm btn-outline-primary" title="View">
                                <i class="bi bi-eye"></i>
                            </a>
                            <a href="/machines/{{ machine.id }}/edit" class="btn btn-sm btn-outline-secondary" title="Edit">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form method="POST" action="/machines/{{ machine.id }}/delete" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this machine?')">
                                <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                                <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="7" class="text-center text-muted py-4">No machines found.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endif %}

<!-- Pagination -->
{% if pagination and pagination.total_pages > 1 %}
<nav class="mt-4">
    <ul class="pagination justify-content-center">
        <li class="page-item {{ pagination.current_page <= 1 ? 'disabled' }}">
            <a class="page-link" href="?page={{ pagination.current_page - 1 }}{{ filters.status ? '&status=' ~ filters.status }}{{ filters.customer_id ? '&customer_id=' ~ filters.customer_id }}{{ filters.machine_type_id ? '&machine_type_id=' ~ filters.machine_type_id }}{{ filters.search ? '&search=' ~ filters.search }}">
                <i class="bi bi-chevron-left"></i>
            </a>
        </li>
        {% for page in 1..pagination.total_pages %}
        <li class="page-item {{ pagination.current_page == page ? 'active' }}">
            <a class="page-link" href="?page={{ page }}{{ filters.status ? '&status=' ~ filters.status }}{{ filters.customer_id ? '&customer_id=' ~ filters.customer_id }}{{ filters.machine_type_id ? '&machine_type_id=' ~ filters.machine_type_id }}{{ filters.search ? '&search=' ~ filters.search }}">{{ page }}</a>
        </li>
        {% endfor %}
        <li class="page-item {{ pagination.current_page >= pagination.total_pages ? 'disabled' }}">
            <a class="page-link" href="?page={{ pagination.current_page + 1 }}{{ filters.status ? '&status=' ~ filters.status }}{{ filters.customer_id ? '&customer_id=' ~ filters.customer_id }}{{ filters.machine_type_id ? '&machine_type_id=' ~ filters.machine_type_id }}{{ filters.search ? '&search=' ~ filters.search }}">
                <i class="bi bi-chevron-right"></i>
            </a>
        </li>
    </ul>
</nav>
{% endif %}
{% endblock %}
